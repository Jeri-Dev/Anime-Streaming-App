@model Application.ViewModels.ProductoraViewModel

@{
    ViewData["Title"] = "Editar Productora";
}

<h3 class="text-light">Editando la Productora: @Model.Name</h3>

<div class="card-body">
    <form method="post" asp-controller="Productora" asp-action="EditAction">
        <div class="mb-3">
            <label asp-for="Name" class="form-label text-light">Nombre: </label>
            <input type="text" asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-primary">Guardar</button>
        <a asp-controller="Productora" asp-action="Index" class="btn btn-danger">Volver</a>
    </form>
</div>
